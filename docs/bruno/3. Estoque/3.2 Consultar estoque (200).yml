info:
  name: "3.2 Consultar estoque (200)"
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/inventory/{{productId}}"
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: before-request
      code: |-
        await bru.sleep(1000); // aguarda a projeção (consistência eventual)
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Projeção atualizada via RabbitMQ. Se retornar 404 logo após a adição, repita."
