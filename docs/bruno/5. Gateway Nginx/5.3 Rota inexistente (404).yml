info:
  name: "5.3 Rota inexistente (404)"
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/nao-existe"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
