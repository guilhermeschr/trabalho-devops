info:
  name: "4.6 Usuário B consulta pedido de A (403)"
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/orders/{{orderId}}"
  auth:
    type: bearer
    token: "{{tokenB}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "403"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "`ORDER_FORBIDDEN`."
